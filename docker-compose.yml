version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: jobapp_mongodb
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
